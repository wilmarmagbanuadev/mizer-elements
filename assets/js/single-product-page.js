jQuery(function($){

	// $c_trigger = $('.sp_styleone .woocommerce-product-gallery__trigger,.sp_stylethree .woocommerce-product-gallery__trigger').clone();
	// $(".sp_styleone .woocommerce-product-gallery__image a,.sp_stylethree .woocommerce-product-gallery__image a").append($c_trigger);
	

	// $.fn.isInViewport = function() {
	// 	var elementTop = $(this).offset().top;
	// 	var elementBottom = elementTop + $(this).outerHeight();

	// 	var viewportTop = $(window).scrollTop();
	// 	var viewportBottom = viewportTop + $(window).height();

	// 	return elementBottom > viewportTop && elementTop < viewportBottom;
	// };

	// $(window).on('resize scroll', function() {
	// 	if ($('.woocommerce-tabs').isInViewport() || $('.related.products').isInViewport()) {
	// 		$('.pd_sidebar').removeClass('bottom');
	// 	}else if($('.style1').isInViewport() || $('.style2').isInViewport() || $('.style3').isInViewport()){
	// 		 $('.pd_sidebar').addClass('bottom');		
	// 	}
	// });


	// if ($(window).width() > 960) {		
	// 	// Start Fixed Side bar in Single Shop Page	
	// 	$(window).scroll(function(){
	// 		if ($(this).scrollTop() > 120) {
	// 			$('.pd_sidebar').addClass('fixed');
	// 		} else {
	// 			$('.pd_sidebar').removeClass('fixed');
	// 		}
	// 	});
		
	// 	$.fn.isInViewport = function() {
	// 		var elementTop = $(this).offset().top;
	// 		var elementBottom = elementTop + $(this).outerHeight();

	// 		var viewportTop = $(window).scrollTop();
	// 		var viewportBottom = viewportTop + $(window).height();

	// 		return elementBottom > viewportTop && elementTop < viewportBottom;
	// 	};
		
	// 	$(window).on('resize scroll', function() {
	// 		if ($('.woocommerce-tabs').isInViewport() || $('.related.products').isInViewport()) {
	// 			$('.pd_sidebar').removeClass('fixed');
	// 		} else {
	// 			// do nothing			
	// 		}
	// 	});
	// 	// End Fixed Side bar in Single Shop Page	

	// }
	// else {
	
	// }
	
});